# Eduardo Chat — Simple Local Chatbot (Flask + Transformers)

A **simple local chatbot web app** built with **Flask + JavaScript** and powered by the open-source model **`Qwen/Qwen2.5-0.5B-Instruct`** (Hugging Face).  
Created as a **final project / course wrap-up** for the **IBM AI Developer** module about **building chatbots and integrating an LLM into a web interface**.

---

## ✨ Features
- Modern single-page chat UI (HTML/CSS/JS)
- Flask backend endpoint: `POST /chatbot`
- LLM responses generated by **Qwen/Qwen2.5-0.5B-Instruct**
- Returns JSON: `{ "response": "..." }`
- Runs locally on **CPU** (lightweight model)

---

## 🧰 Tech Stack
- **Backend:** Python, Flask
- **Model:** `transformers` + `Qwen/Qwen2.5-0.5B-Instruct`
- **Frontend:** HTML, CSS, JavaScript
- **API:** JSON request/response

---

## ▶️ Run Locally (Windows / PowerShell)

Copy and paste everything below:

```powershell
# 1) Create virtual environment
py -m venv my_env

# 2) If PowerShell blocks activation, run this once:
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned

# 3) Activate virtual environment
.\my_env\Scripts\Activate.ps1

# 4) Install dependencies
pip install -r requirements.txt

# 5) Run server
python app.py

# 6) Then open in your browser
start http://127.0.0.1:5000

